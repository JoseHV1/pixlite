services:
  back:
    build:
      context: ./back
    environment:
      - PORT=3000
      - CORS_ORIGIN=*
    ports:
      - "4101:3000"
    restart: unless-stopped

  front:
    build:
      context: ./front
    ports:
      - "4102:80"
    restart: unless-stopped
