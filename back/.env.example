PORT=3000
CORS_ORIGIN=http://localhost:4200

# Notificaciones de error a un canal de Discord (opcional). Si se define,
# cualquier excepción no controlada del backend (5xx) y los errores JS no
# controlados del frontend (reportados via POST /client-error) se envían
# a este webhook.
DISCORD_WEBHOOK_URL=
